rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All client access is denied — backend uses Admin SDK only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
