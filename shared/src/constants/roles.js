/**
 * User Role Constants
 * Defines all valid roles in the Campus Knowledge Agent system.
 */

export const ROLES = {
  STUDENT: 'student',
  FACULTY: 'faculty',
  ADMIN: 'admin',
};

export const ROLE_LIST = Object.values(ROLES);
